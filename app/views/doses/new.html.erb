<div class="container">
<h1>Create a new cocktail</h1>
<%= simple_form_for ([@cocktail, @dose]) do |f| %>
  <%= f.association :ingredient %>
  <%= f.input :description %>
  <%= f.submit "Create your own cocktail", class: "btn btn-primary" %>
<% end %>
</div>
